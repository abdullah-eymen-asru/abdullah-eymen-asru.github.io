# Bu dosya SADECE Cloudflare Pages build'inde kullanılır (build command'da
# --config _config.yml,_config_cloudflare.yml ile birlikte çağrılır).
# Jekyll, iki config dosyası verildiğinde SONRAKİ dosyadaki değerler,
# ÖNCEKİ dosyadaki aynı anahtarların üzerine yazar. Anahtar adları
# _config.yml'dekiyle BİREBİR aynı olmalı, yoksa üzerine yazmaz, ayrı
# ve kullanılmayan bir alan olarak kalır.
#
# url/baseurl artık burada YOK çünkü _config.yml zaten pages.dev'i
# birincil adres olarak gösteriyor — iki dosyada aynı değeri tekrar
# yazmaya gerek kalmadı. Sadece Cloudflare'e özel Analytics ID farklı
# olduğu için burada kalıyor.

google_analytics_id: "G-MZE4HHHH04"
